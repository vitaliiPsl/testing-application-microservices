spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/attempts-db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  redis:
    host: localhost
    port: 6379
server:
  port: 0

tests:
  test-url: http://test-service/api/tests/